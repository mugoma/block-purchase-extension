alert('Hello, world!');

// // Create a timer element
// const timerDiv = document.createElement('div');
// timerDiv.setAttribute('style', 'position: fixed; top: 0; left: 0; background-color: black; color: white; padding: 10px; z-index: 10000;');
// timerDiv.innerHTML = '00:00:00';

// // Add the timer to the body
// document.body.appendChild(timerDiv);

// // Start the timer
// let seconds = 0;
// setInterval(() => {
//   seconds++;
//   const hours = Math.floor(seconds / 3600);
//   const minutes = Math.floor((seconds - (hours * 3600)) / 60);
//   const sec = seconds % 60;
//   timerDiv.innerHTML = `${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${sec.toString().padStart(2, '0')}`;
// }, 1000);
